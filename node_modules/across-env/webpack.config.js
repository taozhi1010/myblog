const path = require('path');

module.exports = {
  mode: 'production',
  entry: {
    'vue-plugin': './src/vue-plugin.js',
    'egg-middleware': './src/egg-middleware.js'
  },
  output: {
    path: path.resolve(__dirname, 'lib'),
    filename: '[name].common.js',
    libraryTarget: 'commonjs2',
  },
  module: {
    rules: [
      {
        test: /\.js$/,
        exclude: [
          path.resolve(__dirname, 'node_modules'),
          path.resolve(__dirname, 'src/egg-middleware.js'),
        ],
        use: {
          loader: 'babel-loader',
          options: {
            presets: ['@babel/preset-env']
          }
        }
      }
    ]
  }
};
